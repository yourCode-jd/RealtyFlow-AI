"use client";
const statuses=["New","Contacted","Qualified","Site Visit Booked","Visit Completed","Negotiation","Won","Lost"];
export default function LeadTable({leads,selectedId,onSelect,onStatusChange}){return <div className="panel table-panel"><div className="panel-heading"><div><p className="eyebrow">Pipeline</p><h2>Leads</h2></div><span className="muted">{leads.length} total</span></div><div className="table-wrap"><table><thead><tr><th>Lead</th><th>Budget</th><th>Score</th><th>Status</th><th>Source</th></tr></thead><tbody>{leads.map(l=><tr key={l.id} className={selectedId===l.id?"selected-row":""} onClick={()=>onSelect(l.id)}><td><strong>{l.name}</strong><span>{l.location}</span></td><td>₹{l.budget}L</td><td><span className={`pill ${l.score.toLowerCase()}`}>{l.score}</span></td><td onClick={e=>e.stopPropagation()}><select className="status-select" value={l.status} onChange={e=>onStatusChange(l.id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td>{l.source}</td></tr>)}</tbody></table></div></div>}
